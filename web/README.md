# Frontend (Спринт 4)

Дашборд: график баланса с красной зоной кассового разрыва + пирог расходов + блок текстовых выводов + загрузка файла.

Стек: React + Recharts (или Go + templ + htmx, если хочется всё на Go).
Кладётся сюда, собирается в статику, раздаётся через Caddy/Go.
